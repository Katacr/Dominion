schema-version: 1
menu-id: dominion_copy
route:
  required: [dominion.name, copy.type]
data:
  entries:
    provider: dominions.copy-sources
action-groups:
  copy:
    actions: ['dominion: copy-from-dominion']
  previous-page:
    actions: ['page: previous']
  next-page:
    actions: ['page: next']
  back:
    actions: ['dominion: back-copy-menu']
