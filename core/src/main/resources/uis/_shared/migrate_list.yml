schema-version: 1
menu-id: migrate_list
data:
  entries:
    provider: residences.migration
action-groups:
  migrate-residence:
    actions: ['dominion: migrate-residence']
  migrate-all:
    actions: ['dominion: migrate-all-residences']
  previous-page:
    actions: ['page: previous']
  next-page:
    actions: ['page: next']
  back-main:
    actions: ['dominion: back-main-menu-route']
